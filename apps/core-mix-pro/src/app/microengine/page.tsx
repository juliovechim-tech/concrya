/**
 * @file app/microengine/page.tsx
 * @description Painel MicroEngine — Microestrutura + Durabilidade
 */

import { PainelMicroEngine } from "../../components/PainelMicroEngine";

export const metadata = {
  title: "MicroEngine — Microestrutura + Durabilidade | CORE MIX PRO",
};

export default function MicroEnginePage() {
  return <PainelMicroEngine />;
}
